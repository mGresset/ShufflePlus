# Déploiement Shuffle+ v9.9.39

```powershell
npm.cmd run validate
git add -A
git commit -m "Release Shuffle+ v9.9.39 - bas du mode conduite accessible"
git push origin main
```

Après publication, fermer complètement Safari, rouvrir Shuffle+, vérifier `v9.9.39`, puis tester le défilement jusqu'aux derniers contrôles du mode conduite.
