# Déploiement Shuffle+ v9.9.14

```powershell
npm.cmd run validate
git add -A
git commit -m "Release Shuffle+ v9.9.14 - header centré et planificateur harmonisé"
git push origin main
```

Après publication, fermer complètement la PWA, la rouvrir et vérifier l’affichage de **v9.9.14**.
