# Déploiement Shuffle+ v9.9.35

```powershell
npm.cmd run validate
git add -A
git commit -m "Release Shuffle+ v9.9.35 - libellé Rechercher stable sur mobile"
git push origin main
```

Après publication, fermer complètement Safari puis rouvrir Shuffle+ et vérifier l’affichage de **v9.9.35**.
