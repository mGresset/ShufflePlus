# Déploiement Shuffle+ v9.9.43

```powershell
npm.cmd run validate
git add -A
git commit -m "Release Shuffle+ v9.9.43 - conduite équilibrée en hauteur"
git push origin main
```

Après publication, fermer complètement Safari puis vérifier que l'en-tête affiche v9.9.43.
