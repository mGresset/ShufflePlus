# Déploiement Shuffle+ v9.9.12

```powershell
npm.cmd run validate
git add -A
git commit -m "Release Shuffle+ v9.9.12 - défilement mobile stable"
git push origin main
```

Après publication, fermer complètement la PWA, la rouvrir et vérifier l’affichage de **v9.9.12**.
