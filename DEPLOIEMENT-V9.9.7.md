# Déploiement Shuffle+ v9.9.8

```powershell
npm.cmd run validate
git add -A
git commit -m "Release Shuffle+ v9.9.8 - verrou visuel Pause Lecture"
git push origin main
```

Après publication, fermer complètement la PWA puis la rouvrir. Vérifier la version 9.9.8 avant le test Pause/Lecture.
