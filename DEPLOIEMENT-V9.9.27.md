# Déploiement Shuffle+ v9.9.27

```powershell
npm.cmd run validate
git add -A
git commit -m "Release Shuffle+ v9.9.27 - retour automatique vers Raccourcis"
git push origin main
```

Après publication, fermer complètement la PWA ou Safari, rouvrir Shuffle+ et vérifier que l’en-tête affiche `v9.9.27`.
