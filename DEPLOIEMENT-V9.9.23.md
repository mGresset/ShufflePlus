# Déploiement Shuffle+ v9.9.23

```powershell
npm.cmd run validate
git add -A
git commit -m "Release Shuffle+ v9.9.23 - personnalisation conduite défilable"
git push origin main
```

Après publication, fermer complètement la PWA, la rouvrir et vérifier que l'en-tête affiche `v9.9.23`.
