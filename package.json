{
  "name": "shuffleplus",
  "version": "8.7.1",
  "private": true,
  "type": "module",
  "description": "Shuffle+ 8.7.1 — recherche globale compacte dans le menu, palette catégorisée et raccourci Ctrl/⌘ K.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node scripts/serve-local.mjs",
    "check": "node scripts/check-js.mjs && node scripts/check-version.mjs && node scripts/check-imports.mjs && node scripts/check-security.mjs",
    "test": "npm run check && node --test tests/*.test.mjs && npm --prefix server test",
    "build": "node scripts/build.mjs",
    "check:dist": "node scripts/check-dist.mjs",
    "validate": "npm test && npm run build && npm run check:dist && npm run smoke",
    "smoke": "node scripts/smoke-local.mjs"
  }
}
