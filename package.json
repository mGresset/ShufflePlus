{
  "name": "shuffleplus",
  "version": "8.3.0",
  "private": true,
  "type": "module",
  "description": "Shuffle+ 8.3 — centre de lancement, raccourci Apple universel, diagnostic guidé et mode conduite enrichi.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node scripts/serve-local.mjs",
    "check": "node scripts/check-js.mjs && node scripts/check-version.mjs && node scripts/check-imports.mjs",
    "test": "npm run check && node --test tests/*.test.mjs && npm --prefix server test",
    "build": "node scripts/build.mjs",
    "check:dist": "node scripts/check-dist.mjs",
    "validate": "npm test && npm run build && npm run check:dist && npm run smoke",
    "smoke": "node scripts/smoke-local.mjs"
  }
}
